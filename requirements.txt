# Core
requests>=2.31        # HTTP fetch (live mode)
selectolax>=0.3        # fast HTML parsing
pandas>=2.0            # cleaning + CSV

# Optional convenience / tests
python-dotenv>=1.0     # load .env automatically (optional TARGET_URL override)
pytest>=8.0            # tests
